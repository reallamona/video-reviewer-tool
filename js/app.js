/* =========================
   Video Reviewer App
========================= */

document.addEventListener(
    "DOMContentLoaded",
    () => {

        console.log(
            "Video Reviewer loaded"
        );


        initializeVideo();
        initializeRemoveVideo();
        initializeYouTube();

        initializeNotes();
        initializeScreenshots();
        initializeFrames();

    }
);
