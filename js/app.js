/* =========================
   Video Reviewer App
========================= */

document.addEventListener(
    "DOMContentLoaded",
    () => {

        console.log(
            "Video Reviewer loaded"
        );


        initializeVideo();
        initializeYouTube();
        initializeNotes();
        initializeScreenshots();
        initializeFrames();

    }
);
